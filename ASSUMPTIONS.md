# 复现假设与范围

本仓库实现的是论文的 CIFAR-10 图像传输侧概念与趋势复现，不是作者官方源码。以下内容用于区分正文明确设置和本实现必须补齐的选择。

## 正文明示设置

| 项目 | CIFAR-10 图像任务 |
| --- | --- |
| 输入 | `3×32×32`，像素范围 `[0,1]` |
| 数据划分 | 50,000 训练 / 10,000 测试 |
| 带宽比 | `1/4`，信道使用数 `N=768` |
| 发射功率 | 每个码字满足 `||z||²=N` |
| 优化器 | AdamW，学习率 `1e-3` |
| batch size | 512 |
| 训练损失 | MSE |
| epoch | 1000 |
| 攻击阈值 | PSNR `≤15 dB`，等价于 MSE `≥10^-1.5` |
| PGA | `alpha=0.1`、`epsilon=1e-8`，首次越阈停止 |
| C&W | Adam `lr=0.01`、`c=1`、范围 `[1e-6,100]`、最多 2000 步 |

## 本实现补齐的假设

1. Table I 只给出输出尺寸，没有完整披露卷积核、padding 和残差块内部结构。本实现使用 `3×3` 卷积、stride-2 下/上采样，以及两个 `3×3` 卷积组成的残差块；核大小在 JSON 中记录。
2. 图像 latent 按 Table I 推为 `2*C_num*8*8`。取 `C_num=6` 得到 768 个实信道使用，恰好对应带宽比 `768/(3×32×32)=1/4`。
3. 训练 SNR 未完整披露。本实现默认固定为 10 dB；`training.snr_db` 也可设成单值、`[min,max]` 或 `min_db/max_db` 区间。该选择会显著改变 clean 曲线和攻击功率。
4. 论文没有给出 PGA 最大步数。本实现正式配置取 2000，默认不对最后一次越阈进行二分精修，以保持“首次越阈即停止”的论文式流程。
5. 论文给出 C&W 的 `c` 上下界，但没有完整说明搜索轮数。本实现使用 9 次二分搜索，每轮最多 2000 步。
6. 论文 Eq. (38) 的符号若直接用于最小化会推动失真下降。本实现使用满足攻击约束含义的 `relu(D* + kappa - D)`，并在结果中记录 `objective_variant=corrected_cw_constraint_hinge`。
7. 论文对 `rho` 同时出现 `||s||²` 与 `||s||²/N` 两种口径。本实现把 `sum_i s_i²` 作为绘图默认值，并同时保存 `mean_i s_i²`，禁止静默混用。
8. 论文没有完整披露随机种子、信道重复次数和失败样本汇总规则。本实现默认种子 2026、每样本一次 AWGN 实现；攻击结果必须同时报告成功率，只在全部样本成功时填写全样本平均攻击功率。

## 不在仓库范围

- 其他数据模态与相关消融；
- 传统 SSCC 链路中的 BPG、DFT top-K、LDPC、QPSK/16-QAM、BP 解码；
- vulnerable-set、GMS/DQN 及传统链路消融；
- 作者未公开的原始训练源码和完整权重。

因此本仓库可以验证图像 DeepJSCC 的运行闭环和经验趋势，但不能独立验证论文对传统通信的倍数比较，也不能把经验攻击结果表述为认证鲁棒性证明。
