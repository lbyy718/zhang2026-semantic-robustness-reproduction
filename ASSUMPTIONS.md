# 复现假设与论文缺口

本目录实现的是论文的**语义通信侧概念复现**，不是作者官方源码。下面区分正文明确给出的设置与实现时必须补齐的假设。

## 正文明确给出的设置

| 项目 | 图像任务 | CSI 任务 |
| --- | --- | --- |
| 输入 | CIFAR-10，`3×32×32`，范围 `[0,1]` | COST2100 outdoor rural，保留 `32×32` angular-delay 矩阵 |
| 数据划分 | 50,000 / 10,000 | 100,000 / 30,000 / 20,000 |
| 带宽比 | `1/4`，`N=768` | `1/4`，`N=256` |
| 发射功率 | `E[||z||²]=N` | 同左 |
| 优化器 | AdamW，学习率 `1e-3` | AdamW，学习率 `1e-3` |
| batch size | 512 | 512 |
| 训练损失 | MSE | MSE |
| epoch | 1000 | 正文未单独说明；本实现沿用 Table I 的 1000 |
| 攻击阈值 | PSNR `≤15 dB`，即 MSE `≥10^-1.5` | NMSE `≥-16 dB`，即线性 NMSE `≥10^-1.6` |
| PGA | `alpha=0.1`，`epsilon=1e-8`，首次越过阈值停止 | 同左 |
| C&W | Adam `lr=0.01`，`c=1`，范围 `[1e-6,100]`，最多 2000 步 | 同左 |

## 本实现补齐的假设

1. Table I 只给输出尺寸，未给卷积核、padding 和残差块内部结构。本实现采用 `3×3` 卷积、stride-2 下/上采样，以及两个 `3×3` 卷积组成的残差块；全部可在 JSON 配置中修改。
2. 图像最终 latent 大小按 Table I 推出为 `2*C_num*8*8`。为得到 `N=768`，取 `C_num=6`；CSI 为得到 `N=256`，取 `C_num=2`。
3. 训练 SNR 未披露。默认固定为 10 dB，配置项 `training.snr_db` 可改成单值或 `[min,max]` 均匀采样区间。不同选择会显著改变 clean 曲线和攻击功率。
4. CSI 的复数表示、归一化和生成器参数未完整披露。CsiNet 作者数据把实部和虚部分别以 `+0.5` 偏移后保存为 2×32×32（2048 维），但本文明确写单通道 32×32、源维度 1024 和 `N=256`。本实现还原复数系数后取幅度，得到论文要求的单通道输入，再用训练集 min-max 归一化；验证/测试严格复用训练集边界。
5. 论文没有给 PGA 最大步数。本实现取 2000；默认不做最后一步二分精修，以保持“首次越阈即停止”。
6. 论文给出 C&W 的 `c` 上下界却未说明搜索轮数。本实现使用 9 次二分搜索，每轮最多 2000 步。
7. 论文 Eq. (38) 的符号若直接最小化会推动失真下降。本实现使用满足约束含义的 `relu(D* + kappa - D)`，并在结果中写入 `objective_variant=corrected_cw_constraint_hinge`。
8. 论文对 `rho` 同时出现 `||s||²` 和 `||s||²/N` 两种记法。主结果默认保存论文攻击段落使用的总能量 `sum_i s_i²`，同时保存每信道使用功率 `mean_i s_i²`，禁止静默混用。
9. 论文没有给随机种子、信道重复次数、失败样本汇总规则。默认种子 2026、每样本一个 AWGN 实现；结果同时报告成功率，只在全部样本成功时填写“全体平均攻击功率”。

## 不在本轮范围

以下代码均属于传统 SSCC 链路，按当前要求暂不实现：BPG、DFT top-K 量化、LDPC、QPSK/16-QAM、BP 解码、vulnerable-set attack、GMS/DQN，以及 Fig. 6/9 的传统链路消融。
